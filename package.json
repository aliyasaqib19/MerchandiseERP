{
  "name": "merchandise-erp",
  "version": "1.0.0",
  "description": "Merchandise ERP",
  "private": true,
  "scripts": {
    "start": "cd server && npm install && npx prisma generate && npx prisma migrate deploy && (node prisma/seed.js || true) && node src/index.js",
    "build": "cd client && npm install && npm run build && mkdir -p ../server/public && cp -r dist/. ../server/public/",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
