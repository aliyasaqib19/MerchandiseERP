{
  "name": "merchandise-erp",
  "version": "1.0.0",
  "description": "Merchandise ERP",
  "private": true,
  "scripts": {
    "start": "cd server && node src/index.js",
    "build": "cd client && npm install && npm run build && xcopy /E /I /Y dist ..\\server\\public",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
